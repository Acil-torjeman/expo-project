// src/pages/exhibitor/Events.jsx
import React from 'react';
import DashboardLayout from '../../layouts/DashboardLayout';

/**
 * Page événements pour l'exposant
 * @returns {JSX.Element} Composant Events
 */
const ExhibitorEvents = () => {
  console.log('Rendering ExhibitorEvents component');
  
  return (
    <DashboardLayout title="Events">
      {/* Le contenu sera vide - seul le layout avec topbar et sidebar sera visible */}
    </DashboardLayout>
  );
};

export default ExhibitorEvents;